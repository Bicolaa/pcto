 #include <stdio.h>

int main ()
{
    int arr[5] = {4 , 56 , 86 , 46, 17};
    int c = 0;
    int b = 1;
    int temp; 

    while(b < 5)
{
    if(a[c] > a[b])
    {
        temp = a[c];
        a[c] = a[b];
        a[b] = temp;
    }
    c= c + 1
}
b = b + 1;
while(b < 7)
{
    printf("%d\n", a[b]);
    b++;
}
}