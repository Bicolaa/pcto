#include <stdio.h>

int main ()
{
    int a;
    int b;
    int c;
    int d;
    int e;
    int f;
    int media;
printf("inserisci un numero \n");
scanf("%d %d %d %d %d %d", &a, &b, &c, &d, &e, &f);
media  = (a + b + c + d + e + f)/6;
printf("numeri inseriti %d\n", media);

}